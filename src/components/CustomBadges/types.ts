/**
 * Common properties shared across all entity badge components.
 */
export interface EntityBadgeProps {
  /** The string content to be displayed inside the badge (e.g., "Mail 1") */
  label: string;
}